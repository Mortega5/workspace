package main

import (
	"producto"
	"fmt"
)
func Productor() {
	for {
	canal:=make(chan int)
		producto.Almacenar(make(chan bool))
	}
}
func main() {
	//fmt.Println(len(almacen))
	canal := make(chan int)

	
}

