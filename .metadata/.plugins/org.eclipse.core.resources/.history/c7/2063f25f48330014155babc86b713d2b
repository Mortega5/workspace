package main

import (
	"producto"
	"fmt"
	"math/rand"
)
func Productor() {
	for {
	canal:=make(chan int)
		producto.Almacenar(canal)
		canal <- math.Rand()
	}
}
func main() {
	//fmt.Println(len(almacen))
	canal := make(chan int)

	
}

