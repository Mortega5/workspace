package main

import (
	"producto"
	"fmt"
)
func Productor() {
	for {
	canal:=make(chan int)
		producto.Almacenar(canal))
	}
}
func main() {
	//fmt.Println(len(almacen))
	canal := make(chan int)

	
}

